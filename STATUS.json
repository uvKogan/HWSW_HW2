{
  "_comment": "Source of truth for project status. Don't hand-edit `parts`/`scenario` here directly -- use `python3 tools/sync_status.py set-part ...` / `set-scenario ...`, which updates this file AND regenerates the matching section of PROJECT.md so the two can't drift. If you do hand-edit this file, a hook regenerates PROJECT.md automatically on save.",
  "team": [
    "Matan Cohen",
    "Yuval Kogan"
  ],
  "last_updated": "2026-07-20",
  "scenario": "Olympic Games Management System",
  "scenario_options": [
    "hospital",
    "hotel",
    "airport",
    "university_courses",
    "olympics",
    "other"
  ],
  "parts": {
    "infra_scaffold": {
      "status": "done",
      "notes": "Generic placeholder operations, not scenario-specific yet"
    },
    "scenario_selection": {
      "status": "done",
      "notes": "Olympic Games Management System: venues/matches/ticketing/volunteers/shuttles/restaurants/subscriptions/country scores, 9 base ops + go_live (Part 3)"
    },
    "part1_traditional": {
      "status": "done",
      "notes": "Real ops wired; ThreadingHTTPServer /invoke mode + threading.Lock seat fix (OLYMPICS_TICKET_LOCK). Verified via correctness gate (MATCH) + seat-race benchmark."
    },
    "part2_faas": {
      "status": "done",
      "notes": "9 base + Part 3 functions as subprocess-per-call stubs; sqlite external state; transactional_apply (OLYMPICS_FAAS_TXN) for the seat fix; go_live_chain orchestrator. Verified MATCH vs Traditional."
    },
    "part3_feature_extension": {
      "status": "done",
      "notes": "go_live cross-cutting cascade (push_live_event fan-out -> allocate_stream -> recompute_standings), pure Python. Naive single-function vs idiomatic 3-invoke orchestrator both shipped. project_medals parallel-throughput op for the FaaS-win axis."
    },
    "part4_performance": {
      "status": "done",
      "notes": "Measured on matanco.space (Linux, multicore): perf stat both architectures, state-growth scaling, seat-race (before/after fix), parallel-throughput (FaaS speedup). Numbers in report/results.md, tables in report."
    },
    "part5_security_maintainability": {
      "status": "optional_not_started",
      "notes": "Team decided to skip (optional per course amendment); effort focused on Parts 1-4 instead"
    },
    "report": {
      "status": "done",
      "notes": "report/report.typ written (4 pages, all Parts + AI disclosure), compiles to PDF via typst. ids.typ takes compile-time ID injection from gitignored report/ids.local. make_submission.sh assembles HW2.zip."
    }
  },
  "blockers": [
    "None. Parts 1-4 + report complete; perf/typst runs done on matanco.space (Linux). Remaining manual step: fill real student IDs in report/ids.local, then rerun make_submission.sh."
  ]
}
