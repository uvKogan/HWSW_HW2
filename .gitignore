__pycache__/
*.pyc
*.db

# compiled accelerator + profiling tooling artifacts
common/cpp/build/
profiling/flamegraph/FlameGraph/
results/*.txt
results/*.data
results/*.svg
results/*.folded
results/*.json
common/workload_fixture.json

# report build output (keep source, ignore compiled unless you want it tracked)
report/*.pdf

# real student IDs — injected at compile time, never committed
report/ids.local

# assembled submission
HW2.zip
submission/
